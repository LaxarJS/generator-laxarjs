<%- banner %>
define( [], function() {
   'use strict';

   return {
      name: '<%= name %>',
      injections: [ 'axContext' ],
      create: function( context ) {

         /* :) */

      }
   };
} );
